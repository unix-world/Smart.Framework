# Language File: EN / Mod-Oauth2
TRANSLATIONS:
  @language: EN
  @area: mod-oauth2
  @version: 2025-07-11
  oauth2:
    yes: 'Yes'
    no: 'No'
    btn-cancel: 'Cancel'
    btn-go-back: 'Go Back'
    ttl-list: 'OAuth2 Manager'
    ttl-list-table: 'OAuth2 Client Management'
    ttl-new: 'OAuth2 Manager - Register New API'
    ttl-reinit: 'OAuth2 Manager - Re-Authorize API'
    ttl-form-new: 'Register New OAuth2 API'
    ttl-form-reinit: 'Re-Authorize OAuth2 API'
    db-fld-id: 'ID'
    db-fld-active: 'Active'
    db-fld-access_expire_seconds: 'Expire Seconds'
    db-fld-access_expire_time: 'Expire DateTime'
    db-fld-scope: 'OAuth2 Scope'
    db-fld-url_auth: 'OAuth2 Auth URL'
    db-fld-url_token: 'OAuth2 Token URL'
    db-fld-url_redirect: 'Redirect URL'
    db-fld-client_id: 'Client ID'
    db-fld-client_secret: 'Client Secret'
    db-fld-code: 'OAuth2 Code'
    db-fld-access_token: 'Access Token'
    db-fld-refresh_token: 'Refresh Token'
    db-fld-id_token: 'JWT-ID Token'
    db-fld-description: 'Description'
    db-fld-logs: 'Operations Log'
    db-fld-errs: 'Success / Errors #'
    db-fld-account: 'Account'
    db-fld-modified: 'Last Updated'
    db-fld-created: 'Created'
    hint-frm-id: 'A Custom, Unique ID ; Validation'
    hint-frm-description: |
        Description
        [Should not contain sensitive information, will not be encrypted]
    hint-frm-client_id: 'OAuth2 Client ID (Application ID)'
    hint-frm-client_secret: 'OAuth2 Client Secret'
    hint-frm-scope: 'OAuth2 Scopes ; Example'
    hint-frm-url_redirect: 'OAuth2 Redirect URL ; Example'
    hint-frm-url_auth: 'OAuth2 Authorization URL ; Example'
    hint-frm-url_token: 'OAuth2 Token URL ; Example'
    hint-frm-code: 'Enter the OAuth2 Code (from Step 1)'
    hint-tbl-op: 'Display / Refresh / Delete'
    hint-fld-id: 'Unique ID'
    hint-fld-active: 'Active or Inactive Status'
    hint-fld-access_expire_seconds: 'Expire after Seconds since Last Update'
    list-frm-filter: 'Filter'
    list-frm-reset: 'Reset'
    list-col-status-active: 'Status is ACTIVE'
    list-col-status-inactive: 'Status is INACTIVE'
    list-col-refresh-token-true: 'The API provides a Refresh Token'
    list-col-refresh-token-false: 'The API does NOT support a Refresh Token'
    list-col-jwt-id-true: 'The API provides an Open-Id JWT'
    list-col-jwt-id-false: 'The API does NOT provide an Open-Id JWT'
    double-click: 'Double-Click to Change'
    validation-pattern: 'Validation Pattern'
    validation-regex: 'Validation Regex'
    example: 'Example'
    expired: 'Expired'
    errors: 'Errors'
    display-ttl: 'OAuth2 API - Details'
    display-tk-valid: 'Token is VALID'
    display-tk-expired: 'Token is EXPIRED'
    display-btn-reauth: 'Re-Authorize OAuth2 API'
    display-reauth: 'This action will run a full Re-Authorization of the current OAuth2 API and will Refresh the Access-Token too. Use this operation in case that the Refresh-Token has not been provided and the Access-Token expired or in case of troubles with the Access Token updates ...'
    display-only-active-reauth: 'Only active APIs may be Re-Authorized ...'
    display-secret: 'Secret'
    display-size: 'Size'
    display-tk-no-refresh: 'Token does Not Support Refresh (if Access-Token is expiring, needs Re-Authorize)'
    display-id-jwt: 'ID JWT'
    display-id-jwt-none: 'The API does not provide an ID JWT'
    display-btn-token: 'Display the Access Token'
    display-btn-token-exp: 'Get a fresh Access Token'
    display-btn-token-hint1: 'Click here to Display (or Refresh, if expired) the Access Token.'
    display-btn-token-hint2: 'This OAUth2 API does NOT Support Refresh (did not provided a Refresh Token). To Refresh this Token use Re-Authorize OAuth2 API (click on Re-Authorize button below).'
    display-refresh-question: 'Refresh now the Tokens ?'
    display-refresh-hint: 'The Access Token is automatically refreshed if expired at every usage, thus may not necessary to manually update it. Anyway you can do update the Access Token now if press OK, but the OAuth2 Server may not return a new one until this one expires...'
    display-refresh-ttl: 'Access Token Manual REFRESH'
    display-refresh-cannot: 'May not be Refreshed, either does not support Refresh or the API is inactive'
    display-refresh-cannot-hint: 'Only active API Tokens that support Refresh may be Refreshed ...'
    display-refresh-explain: 'This operation will explicit Refresh the current Access Token even if is Not Expired. Normally the `Get a fresh Access Token` operation should be used instead of this one, because that one will return the Current Access Token (if Not Expired) or a Fresh / New Access Token (if Expired). Use this operation in case of troubles with the Access Token updates only or a Refresh Token does not exists.'
    display-refresh-btn: 'Refresh Tokens'
    display-delete-question: 'Delete now this OAuth2 API ?'
    display-delete-hint: 'The OAuth2 API will be completely removed. Press OK to confirm the deletion.'
    display-delete-ttl: 'OAuth2 API DELETE'
    display-delete-cannot: 'This OAuth2 API can not be deleted, it is active'
    display-delete-cannot-hint: 'Use this operation to DELETE the selected OAuth2 API. Alternatively, you can consider to just set it as INACTIVE by double-clicking the checkbox (button) in the Listing Table area, instead to delete it ...'
    display-delete-btn: 'Delete Oauth2 API'
    display-notice: 'Notice'
    display-empty-value: 'Value is Empty'
    token-display-ttl: 'OAuth2 Access Token'
    token-status: 'Status'
    token-id: 'ID'
    token-label: 'Token'
    copy-to-clipboard: 'Copy to Clipboard'
    copy-to-clipboard-ok: 'Token has been Copied to Clipboard'
    copy-to-clipboard-err: 'FAILED to Copy the Token to Clipboard'
    api-init: 'Initialize OAuth2 API'
    api-init-ok: 'OAuth2 Client Initialization Done'
    api-init-err: 'OAuth2 Client Initialization Failed, Errors'
    api-reinit: 'Re-Initialize OAuth2 API'
    api-reinit-ok: 'OAuth2 Client Re-Initialization Done'
    api-reinit-err: 'OAuth2 Client Re-Initialization Failed, Errors'
    api-delete: 'Deleting the OAuth2 API'
    api-token-refresh: 'Refreshing the Access Token for OAuth2 API'
    api-edit: 'Modify API Field'
    api-edit-ok: 'Updated'
    api-edit-err: 'FAILED to Update'
    api-edit-xx: 'Data field is not editable'
    code-exch-ttl: 'OAuth2 Client :: Code Exchange'
    code-exch-head: 'OAuth2 New Code :: API response'
    code-exch-confirm: |
        Make sure you Copy the Code before closing this window, use the Code in the OAuth2 Form.
        *
        CLOSE THIS WINDOW NOW ?
    code-exch-srv-dtime: 'Server DateTime'
    code-exch-csrf-ok: 'CSRF State Verification SUCCESS ...'
    code-exch-csrf-err: |
        CSRF State Verification FAILED, the `State` parameter is empty or missing from reply.
        The `OAuth2-Code` may be valid or not. If it does not work, retry the entire operation ...
    code-exch-code-oauth2: 'OAuth2 Code'
    code-exch-code-copy-btn: 'Copy Code'
    code-exch-go-back-btn: 'Go Back'
#END